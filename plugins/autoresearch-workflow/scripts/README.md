# Autoresearch Workflow Scripts

This plugin intentionally does not ship a second workflow runner.

Future scripts in this directory should be thin transport adapters that call the repo CLI commands
listed in `skills/workspace-flow-operator/SKILL.md`.
