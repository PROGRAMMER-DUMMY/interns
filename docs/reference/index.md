# Reference — Index

Active, ground-truth reference material for building the platform: data-engineering patterns,
Databricks/lakehouse production practices, and agentic-CLI tooling notes. These are consumed by
agents/code or used as ground truth during work — not per-customer output.

| File | What it is | Use for |
|------|------------|---------|
| `data_workflow_medallion_reference.md` | Medallion (bronze/silver/gold) pipeline reference: ingestion, cleaning, serving, modern tooling | Pipeline architecture; quality-tier and serving patterns |
| `databricks_production_practices.md` | Databricks production patterns: Unity Catalog, Lakeflow, Auto Loader, Delta maintenance, SQL warehouses, CI/CD, security, cost, observability | Databricks/Unity Catalog production work |
| `databricks_token_scopes.md` | Every Databricks OAuth/PAT scope + which the project needs; wired into `tools/databricks_setup.py` | Minimum-scope token generation; per-task scope mapping |
| `self_hosted_lakehouse_ops_reference.md` | Self-hosted lakehouse ops: configs, templates, sizing, anti-patterns for a MinIO + Spark + Airflow + Trino stack | Self-hosted infra ops; grounds the PySpark generator's lakehouse rules |
| `dbt_agentic_cli_reference.md` | How an agentic CLI uses dbt as a tool (not a web-app guide) | dbt-as-a-tool usage from a CLI agent |
| `dbt_cli_reference.md` | Operator-grade dbt CLI reference: full command/flag inventory from real `--help` output, node selection + slim-CI state/defer, microbatch backfills, artifacts (manifest/run_results/sources/catalog), dbt-databricks strategies + query tags + python submission, exit codes and silent-failure traps — verified against the repo venv (dbt-core 1.11.12 / dbt-databricks 1.12.2) | Wiring generated dbt projects, Cosmos/Airflow tasks, WAP publish, backfills, ghost-table reconcile and cost attribution |
| `senior_data_engineer_patterns.md` | Real senior/staff DE day-to-day patterns: on-call triage, slow-pipeline diagnosis, lineage, dimensional modeling vs. OBT, schema drift, cost, Airflow-at-scale, idempotency, requirement changes, cross-team contracts — source-audited with confidence notes | Grounding design decisions in real operational practice, not assumption |
| `kpi_column_concept_mapping_research.md` | How established practice handles "map to concept, not to column": Rahm/Bernstein schema-matching taxonomy + COMA/Cupid/Similarity Flooding, dbt Semantic Layer/MetricFlow + LookML + Cube.dev + AtScale, Featuretools/Feast/Tecton feature derivation, Kimball/dbt/Looker metric governance + proxy-substitution cautionary examples — source-audited with confidence notes and per-section "more/less rigorous than this protocol" callouts | Grounding the feature_resolver/schema_alias_matching redesign around canonical concepts, multi-signal confidence tiers, and derivable-vs-proxy gating |

| `databricks_cli_reference.md` | Operator-grade Databricks CLI reference, probed for real on this machine (`databricks --version` v1.7.0, the Go unified CLI): `--help` for every group and the commands that matter, plus harmless reads (`current-user me`, `catalogs list`, `warehouses list`, `auth describe`) and a locally rendered `bundle init dbt-sql` + `bundle validate`. Hosts/tokens/IDs are redacted throughout | Unity Catalog, warehouse, bundle and auth work from the CLI; knowing which command exists before improvising SDK calls |
| `airflow_cli_reference.md` | Operator-grade Apache Airflow 3.x CLI + Astro CLI reference, mapped onto this repo's orchestration surface (`core/orchestration/airflow_dag.py`, `cosmos_dag.py`, `dbt_backfill.py`), with the locally verified state of `airflow`/`astro` on this box | Generating and operating Airflow/Cosmos DAGs, backfills, local dev via astro |
| `engine_roles_at_scale_research.md` | What the evidence says about dividing work between warehouse SQL (dbt), PySpark, and single-node engines in a Databricks + dbt + Airflow stack at TB scale, and what cross-engine parity should mean | Deciding which engine owns which transform; arguing about parity with sources, not instinct |
| `engine_compute_selection_research.md` | How to choose ONE execution engine per workspace pipeline and the compute tier under it, from measured data properties plus a small question set; documents what the existing per-KPI `engine_recommender.py` heuristic does and does not measure | Replacing or grounding per-KPI engine guessing with working-set and SLA-driven selection |
| `end_users_data_modeling_research.md` | Consumer-class to design-delta matrix, a concrete intake question list, and a decision table that picks a modeling technique (lakehouse: Databricks UC + dbt + Airflow); deliberately does not repeat `vendor/minus_dataops` depth dossiers | Intake questions about who consumes the data, and picking star/OBT/SCD design from the answer |
| `data_quality_frameworks_research.md` | DQ framework landscape for a Databricks + dbt stack: dbt tests (+dbt-expectations/Elementary) at the model layer, Databricks-native expectations (Lakeflow/DQX, UC data-quality monitoring) at the bronze boundary, and which frameworks not to adopt | Choosing where a given check belongs; profile-derivable checks vs must-ask business rules |
| `pipeline_practices_gap_research.md` | Gaps beyond `senior_data_engineer_patterns.md`, framed for a platform that *generates* dbt + Airflow(Cosmos) + Databricks pipelines: backfill primitives across all three, catchup foot-guns, event-trigger traps, WAP-on-Delta, ghost tables, cost attribution via query tags | Reviewing generated DAG/model defaults before they ship |
| `chart_selection_guide.md` | Knowledge source behind `core/dashboard/chart_knowledge.py`: the data-to-viz decision-tree subset that applies to pre-aggregated KPI result views | Dashboard panel/chart-type decisions |
| `voltagent_platform_audit_2026-08-05.md` | Merged, deduplicated, severity-ranked findings from three independent read-only VoltAgent reviews (pipeline / storage-query / consumption) of the cloud-first platform, each with code evidence; items already tracked in the cloud-first restructure plan were excluded | Picking up known platform defects with their evidence instead of re-discovering them |

When adding a file here: add one row above, then confirm `docs/README.md` still points at this
folder (it points at the folder, not individual files — keep it that way).
